# i3-like starter config for Hyprland.
{{- if eq .chezmoi.hostname "nixos" }}
# Hyprland uses wlroots output names (for example DP-1, HDMI-A-1), not Xorg-style DP-0/HDMI-0.
# Keep this stable across reboots by explicitly placing likely outputs and allowing fallback auto config.
monitor=DP-1,preferred,0x0,1
monitor=HDMI-A-1,preferred,2560x180,1
monitor=,preferred,auto,1
{{- else }}
monitor=,preferred,auto,1
{{- end }}

$mod = SUPER
$terminal = alacritty
$menu = rofi -show drun

exec-once = waybar
exec-once = dunst
exec-once = nm-applet --indicator

input {
  kb_layout = us
  follow_mouse = 1
  touchpad {
    tap-to-click = true
    natural_scroll = false
  }
}

general {
  gaps_in = 6
  gaps_out = 12
  border_size = 2
  layout = dwindle
}

decoration {
  rounding = 8
  blur {
    enabled = false
  }
}

animations {
  enabled = true
}

dwindle {
  pseudotile = true
  preserve_split = true
}

misc {
  disable_hyprland_logo = true
}

ecosystem {
  no_update_news = true
  no_donation_nag = true
}

bind = $mod, Return, exec, $terminal
bind = $mod, D, exec, $menu
bind = $mod SHIFT, Q, killactive
bind = $mod SHIFT, E, exit
bind = $mod, F, fullscreen, 1
bind = $mod SHIFT, space, togglefloating
bind = $mod, P, pseudo

# i3-style directional focus with hjkl.
bind = $mod, H, movefocus, l
bind = $mod, L, movefocus, r
bind = $mod, K, movefocus, u
bind = $mod, J, movefocus, d

# Move windows with Shift+hjkl.
bind = $mod SHIFT, H, movewindow, l
bind = $mod SHIFT, L, movewindow, r
bind = $mod SHIFT, K, movewindow, u
bind = $mod SHIFT, J, movewindow, d

# Workspaces 1-10 (0 == 10) like i3.
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9
bind = $mod, 0, workspace, 10

bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9
bind = $mod SHIFT, 0, movetoworkspace, 10

# Mouse drag with modifier, similar to i3 floating move/resize ergonomics.
bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
